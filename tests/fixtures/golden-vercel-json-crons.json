{
  "description": "Golden snapshot: Read vercel.json with crons key triggers cron-jobs via key-aware routing",
  "input": {
    "tool_name": "Read",
    "tool_input": { "file_path": "__TEMP_VERCEL_JSON__" }
  },
  "vercelJson": {
    "crons": [{ "path": "/api/cron", "schedule": "0 * * * *" }]
  },
  "expected": {
    "skillInjection": {
      "version": 1,
      "toolName": "Read",
      "matchedSkills": ["routing-middleware", "deployments-cicd", "vercel-cli", "cron-jobs", "vercel-functions"],
      "injectedSkills": ["cron-jobs", "vercel-cli", "vercel-functions"],
      "droppedByCap": ["deployments-cicd", "routing-middleware"],
      "droppedByBudget": []
    }
  }
}
