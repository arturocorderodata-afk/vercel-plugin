{
  "description": "Golden snapshot: Bash 'npm install ai @vercel/analytics @vercel/flags @vercel/workflow' triggers 4 skills, cap drops workflow",
  "input": {
    "tool_name": "Bash",
    "tool_input": { "command": "npm install ai @vercel/analytics @vercel/flags @vercel/workflow" }
  },
  "expected": {
    "skillInjection": {
      "version": 1,
      "toolName": "Bash",
      "toolTarget": "npm install ai @vercel/analytics @vercel/flags @vercel/workflow",
      "matchedSkills": ["vercel-flags", "observability", "workflow", "ai-sdk"],
      "injectedSkills": ["ai-sdk", "observability", "vercel-flags"],
      "droppedByCap": ["workflow"],
      "droppedByBudget": []
    }
  }
}
